{
  vim = {
    autocomplete.blink-cmp = {
      enable = true;
      mappings = {
        close = "<C-e>";
        confirm = "<C-y>";
        next = "<C-n>";
        previous = "<C-p>";
      };
    };

    snippets.luasnip.enable = true;
  };
}
